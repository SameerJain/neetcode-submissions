class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        seenNums = set();
        for num in nums:
            if num == seemNums:
                return True
            seenNums.append(num)
        return false
         